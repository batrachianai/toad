# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
__pycache__

# Virtual environments
.venv
.DS_Store
.vscode
.zed
.git
.mypy_cache
*.github
*.log

uv.lock
agent.jsonl
replay.jsonl
sandbox/

# DEGA Core — orchestrator runtime state (ephemeral, not tracked)
/.orchestrator/

# DEGA Core — personal focus config for planner loop
focus.yaml

# Local scratch: personal notes, runbooks, test checklists — not for commit
.local/

# Impeccable upstream source repo — clone locally if you want the full toolkit.
# Only the lean per-skill extracts (impeccable-audit, impeccable-clarify, ...)
# are tracked; the 11MB source tree is not.
.claude/skills/impeccable/
